// component that contains the functionalities that appear on top of
// the pharmacies table: create pharmacy

window.TopActionsComponent = React.createClass({
    render: function(){
        return (
            <div>
                <a href='#'
                    onClick={() => this.props.changeAppMode('findOne')}
                    className='btn btn-primary margin-bottom-1em'> Find pharmacy
                </a>
            </div>
        );
    }
});